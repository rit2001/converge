# Failure tests

Milestone 1 client gap and duplicate cases are unit/integration tested. Process crash, outbox replay,
Redis interruption, and snapshot recovery scenarios will be added with their corresponding features.
