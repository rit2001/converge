{
  "name": "@converge/integration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@converge/api": "workspace:*",
    "@converge/canvas-engine": "workspace:*",
    "@converge/database": "workspace:*",
    "@converge/protocol": "workspace:*",
    "@converge/testkit": "workspace:*",
    "pg": "8.16.3"
  },
  "devDependencies": {
    "@types/node": "22.18.0",
    "@types/pg": "8.15.5",
    "vitest": "3.2.4"
  }
}
